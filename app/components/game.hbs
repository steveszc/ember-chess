<Announcer
  @turnColor={{this.turnColor}}
  @previousTurnInfo={{this.previousTurnInfo}}
/>

<Surface
  @gameMode={{this.gameMode}}
  @isShowingKey={{this.isShowingKey}}
  @isShowingGuide={{this.isShowingGuide}}
  @turn={{this.turn}}
  @turnColor={{this.turnColor}}
>
  <Board
    @board={{this.board}}
    @turnColor={{this.turnColor}}
    @incrementTurn={{this.incrementTurn}}
    @gameMode={{this.gameMode}}
  />
  <Side
    @color="white"
    @capturedPieces={{this.board.whiteCapturedPieces}}
  />
  <Side
    @color="black"
    @capturedPieces={{this.board.blackCapturedPieces}}
  />
</Surface>

<Settings
  @resetGame={{this.resetGame}}
  @isShowingKey={{this.isShowingKey}}
  @toggleKey={{this.toggleKey}}
  @gameMode={{this.gameMode}}
  @setGameMode={{this.setGameMode}}
  @isShowingGuide={{this.isShowingGuide}}
  @toggleGuide={{this.toggleGuide}}
  @isShowingLog={{this.isShowingLog}}
  @toggleLog={{this.toggleLog}}
  @fen={{this.fen}}
  @updateFen={{this.updateFen}}
/>